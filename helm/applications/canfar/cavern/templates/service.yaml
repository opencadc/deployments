apiVersion: v1
kind: Service
metadata:
  name: {{ include "cavern.fullname" . }}
  labels:
    {{- include "cavern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cavern.selectorLabels" . | nindent 4 }}
