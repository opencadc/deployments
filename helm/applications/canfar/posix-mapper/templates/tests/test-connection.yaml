apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "posix-mapper.fullname" . }}-test-connection"
  labels:
    {{- include "posix-mapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "posix-mapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
