apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storage-ui.fullname" . }}-test-connection"
  labels:
    {{- include "storage-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storage-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
