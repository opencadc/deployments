apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage-ui.fullname" . }}
  labels:
    {{- include "storage-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storage-ui.selectorLabels" . | nindent 4 }}
