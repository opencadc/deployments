apiVersion: v1
kind: Service
metadata:
  name: {{ include "skahaX.fullname" . }}
  labels:
    {{- include "skahaX.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skahaX.selectorLabels" . | nindent 4 }}
