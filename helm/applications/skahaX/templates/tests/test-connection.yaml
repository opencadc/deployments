apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skahaX.fullname" . }}-test-connection"
  labels:
    {{- include "skahaX.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skahaX.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
