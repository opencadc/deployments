apiVersion: v1
kind: Service
metadata:
  name: {{ include "canfar-web-root.fullname" . }}
  labels:
    {{- include "canfar-web-root.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "canfar-web-root.selectorLabels" . | nindent 4 }}
