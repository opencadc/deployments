apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "canfar-web-root.fullname" . }}-test-connection"
  labels:
    {{- include "canfar-web-root.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "canfar-web-root.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
