{{- $mb := index .Values "metrics-backend" | default dict }}
{{- $rbac := $mb.rbac | default dict }}
{{/* Kueue read ClusterRole/RBAC. If created outside this chart, set metrics-backend.rbac.create to false. */}}
{{- if and (default false $mb.enabled) $rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "skaha.metricsBackend.clusterRoleName" . }}
  labels:
    {{- include "skaha.metricsBackend.labels" . | nindent 4 }}
rules:
  - apiGroups: ["kueue.x-k8s.io"]
    resources: ["clusterqueues", "cohorts"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "skaha.metricsBackend.clusterRoleName" . }}
  labels:
    {{- include "skaha.metricsBackend.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "skaha.metricsBackend.clusterRoleName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.deployment.skaha.serviceAccountName }}
    namespace: {{ .Release.Namespace }}
{{- end }}
