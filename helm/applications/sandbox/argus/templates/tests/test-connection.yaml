apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argus.fullname" . }}-test-connection"
  labels:
    {{- include "argus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
