apiVersion: v1
kind: Service
metadata:
  name: {{ include "sc2meta.fullname" . }}
  labels:
    {{- include "sc2meta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sc2meta.selectorLabels" . | nindent 4 }}
