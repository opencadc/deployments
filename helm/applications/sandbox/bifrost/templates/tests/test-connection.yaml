apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bifrost.fullname" . }}-test-connection"
  labels:
    {{- include "bifrost.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bifrost.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
