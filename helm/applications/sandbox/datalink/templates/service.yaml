apiVersion: v1
kind: Service
metadata:
  name: {{ include "datalink.fullname" . }}
  labels:
    {{- include "datalink.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datalink.selectorLabels" . | nindent 4 }}
