apiVersion: v1
kind: Service
metadata:
  name: {{ include "ac.fullname" . }}
  labels:
    {{- include "ac.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ac.selectorLabels" . | nindent 4 }}
